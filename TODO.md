if styled(QuarkComponent) -> inherit VariantMap to use in default values
  - defaultValues will pass through the child QuarkComponent

isQuarkComponent()

QuarkComponent.variantMap <-- put this in the QuarkComponent