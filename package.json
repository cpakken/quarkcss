{
  "name": "quarkcss",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "pnpm -r test run",
    "build": "turbo build",
    "publish-packages": "turbo test build && changeset version && changeset publish"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.25.2",
    "turbo": "^1.6.3"
  }
}
