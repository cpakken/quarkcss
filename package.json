{
  "name": "quarkcss",
  "private": true,
  "version": "0.0.2",
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test run"
  },
  "workspaces": [
    "packages/*"
  ]
}
